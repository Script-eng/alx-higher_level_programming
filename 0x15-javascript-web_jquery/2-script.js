// Use jQuery to add a click event handler to the div with ID 'red_header'
$('#red_header').click(function() {
  // Update the text color of the header to red when the div is clicked
  $('header').css('color', '#FF0000');
});
